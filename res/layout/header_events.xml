<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/header_background"
    android:id="@+id/header">
    
    <ImageView android:id="@+id/image_newsHeader" 
        android:contentDescription="@string/descripcion"
         android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginRight="10dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_launcher"
        android:background="@color/header_background_image"/>

    <TextView
        android:id="@+id/text_eventHeader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="15dp"
        android:layout_toRightOf="@+id/image_newsHeader"
        android:text="Evento"
        android:textColor="@color/header_text" />

    <ImageView
        android:id="@+id/image_newsHeaderCalendar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:src="@drawable/evento50" />

</RelativeLayout>